windows.addEventlistener('DOMContentLoaded', (event) => {
    getVisitCount();
})


const function = '';

const getVisitCount = () => {
    let count = 30
    fetch(funtionApi).then(response => {
        return response.json()
    }).then(response log(...data: any[]): void
        console.log("Website called funtion API.");
    count = response.count;
    document.getElementById("counter").innerText = count;
}).catch (funtion)(error);
console.log(error);
});
return count;

}